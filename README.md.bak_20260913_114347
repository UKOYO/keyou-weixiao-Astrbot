# 星渊 API 监控与工具账本（astrbot_plugin_syuan_monitor）

监控星渊 API（Syuan API / OneAPI）中转站的 Token 消费额度、模型调用分布、请求耗时，并分类统计 AstrBot 本地各项工具与插件的调用次数、花费估算及驱动 LLM。

顺带把中转站账号侧的每日签到、兑换码也一起接管了：自动签到、公告捡码、一键试兑。

作者：唯笑 & keyou · 版本 v1.2.0

---

## 功能特性

1. **中转站消费监控**：
   - 自动读取 `cmd_config.json` 中配置的星渊 API Key，拉取调用日志；也可在插件配置里手填 API Key，或再配一个账号访问令牌 + 用户 ID 换取账号级全量日志。
   - 统计中转总调用量、总花费估算（按 1 USD ≈ 500,000 Quota 换算）与平均响应延迟。
   - 模型维度明细：各模型的调用次数、总花费与平均响应时间。
   - 调用流水：展示每次调用的时间、模型、耗时、Prompt/Completion Token 及具体扣费。
   - **自动翻页补齐**：上游把单页 `page_size` 夹在 100 条，插件会自动连续翻页拼接，所以「昨天全天 / 今日全天」这类大窗口不会再只拿到一页就被截断。
   - **条数不限**：`log_page_size` 与 `daily_page_size` 填 `-1` 即表示不限条数，一路翻页到上游给不出新数据为止。

2. **插件与工具分类手账**：
   - 自动解析 AstrBot 本地日志（`astrbot.log`）中的工具触发事件。
   - 中文友好显示：将内部工具 ID 映射为中文插件与功能名，并在下方标注底层英文 ID。
   - 驱动 LLM 标注：清晰列出每个工具执行或决策时所使用的主要大模型（如 `gemini-3.8-flash`、`gemini-3.7-flash (VLM)` 等）。
   - 统计每个工具的触发频次、消耗上下文 Token 估算与折算成本。

3. **数据只读不落盘**：
   - 每次统计都直接从中转站接口实时拉取，不在本地写入任何日志副本或历史缓存文件。
   - 仅保留数秒内存缓存用于削峰防抖（由 `refresh_seconds` 控制），进程重启即自动清空。

4. **报错尖峰提醒（可选）**：
   - 开启后按固定间隔巡检，最近十分钟报错条数超过阈值时主动私聊播报，并带冷却时间避免刷屏。
   - 播报内容含状态码归因、受影响模型、涉及令牌与最新几条明细，方便一眼定位。
   - 推送对象与日报共用一份白名单（`push_users`），多人时逐个投递，单人失败不影响其他人。

5. **签到与兑换码**：
   - 每天按 `auto_checkin_time`（默认 00:05）自动签到一次，到账额度会折算成金额一并播报；当天站点已签过则自动跳过。
   - 也可手动 `签到` 补签，`签到状态` 查看连签天数、累计次数、累计额度与最近几条记录。
   - 兑换码两种玩法：`兑换码 <码>` 直接兑；插件还会从站点公告里识别疑似兑换码（32 位十六进制 / 公告口令形态），用 `一键兑换` 逐个试兑，已兑过的码会记在本地自动跳过，不会重复提交。

6. **双端查看与呈现**：
   - **聊天指令与手账卡片**：支持发送 `api账本` / `api统计` / `api监控`，实时渲染温馨手账风格的卡片长图。
   - **每日自动推送**：每天清晨 06:00 自动生成前一日消费与调用手账，连同当前余额按白名单推送。
   - **后台可视化 WebUI**：内置 Dashboard 扩展页面，可直接在 AstrBot 管理面板打开交互式表格，支持关键词即时筛选与「今日 / 昨日」时间窗切换。
   - **排版优化**：修正文字被侧栏挤压重叠的问题，过长的名称与模型 ID 会自动折行或省略，保证卡片与表格都能完整读完。

---

## 页面访问 (WebUI)

本插件符合 AstrBot 官方 WebUI 规范：
- 进入 AstrBot 管理后台 -> **插件** 列表 -> 找到 **星渊账本 (astrbot_plugin_syuan_monitor)**。
- 点击插件拓展页面的 **pages/dashboard** 即可进入可视化大屏。

---

## 指令列表

| 指令 | 别名 | 说明 |
| --- | --- | --- |
| `api账本` | `api统计`、`api监控` | 生成并发送手账风格的 API 消费与工具统计长图 |
| `api报错` | `api错误`、`api故障` | 只看中转站报错流水：报错占比、近一小时条数、状态码 / 模型 / 令牌归因，附最新几条明细 |
| `签到状态` | `查询签到` | 查看今日是否已签、连签天数、累计次数、累计到手额度与最近 5 条签到记录 |
| `签到` | `星渊签到` | 手动执行一次星渊签到，返回到手额度并折算成金额；今天已签会直接告诉你 |
| `兑换码 <码>` | `兑换 <码>` | 带码即兑换并播报到账额度；**不带码**则列出站点公告里捡到的兑换码，并标注哪些已兑过 |
| `一键兑换` | `捡公告码` | 把公告里捡到的码逐个试兑，已兑过的自动跳过，最后汇总成功 / 失败清单 |

> 提示：`兑换码` 和 `一键兑换` 都依赖账号访问令牌 + 用户 ID（`access_token` / `user_id`）；只配了 API Key 时，签到和兑换会提示读不到账号信息。

---

## 自动任务

| 任务 | 触发时机 | 说明 |
| --- | --- | --- |
| 每日账本日报 | 每天 06:00 | 统计前一日消费与调用，附当前余额，按 `push_users` 白名单推送 |
| 自动签到 | 每天 `auto_checkin_time`（默认 00:05） | 由 `auto_checkin_enabled` 控制，成功后按白名单播报到账额度 |
| 报错尖峰巡检 | 每 `alert_interval_seconds` 秒（默认 300） | 由 `alert_enabled` 控制，近十分钟报错达到 `alert_threshold` 时提醒，冷却 `alert_cooldown_seconds` |

---

## 配置项速览

| 配置项 | 说明 |
| --- | --- |
| `api_base` | 中转站地址，默认 `https://api.syuan.org` |
| `access_token` / `user_id` | 账号级访问令牌与用户 ID，用于拉全量日志、签到与兑换 |
| `api_key` | 星渊 API Key，留空时自动从 `cmd_config.json` 抓取 |
| `refresh_seconds` | 内存缓存秒数，用于削峰防抖 |
| `log_page_size` | 每次拉取的日志条数，`-1` 表示不限 |
| `daily_page_size` | 每日日报拉取的日志条数，`-1` 表示不限 |
| `error_log_cap` | 报错日志展示上限，`0` 表示不展开明细 |
| `page_size` | WebUI 表格每页条数 |
| `daily_flow_rows` | 日报卡片里的流水条数，`0` 表示只留汇总 |
| `push_users` | 推送白名单（每日 06:00 账本卡片、报错尖峰提醒、签到播报共用）：QQ 号，多个用逗号或换行分隔；留空则只推给默认用户 |
| `daily_push_users` | 旧字段（已并入 `push_users`），仅在 `push_users` 留空时生效 |
| `alert_enabled` | 开启报错尖峰私聊提醒 |
| `alert_interval_seconds` | 尖峰巡检间隔（秒），最小 60 |
| `alert_threshold` | 触发提醒的近十分钟报错条数阈值 |
| `alert_cooldown_seconds` | 两次提醒之间的最短冷却（秒），最小 300 |
| `auto_checkin_enabled` | 开启每日自动签到 |
| `auto_checkin_time` | 自动签到时刻（`HH:MM`），默认 `00:05` |
| `theme` | 卡片主题：`dark` 暗黑 / `light` 纯白 |

---

## Web API 接口

路由前缀为 `/astrbot_plugin_syuan_monitor/api`（经 Bridge 安全调用）：

- `GET /overview`：总调用、总花费、平均延迟、缓存更新时间等总览。
- `GET /models`：各模型的调用次数、总花费与平均耗时列表。
- `GET /tools`：各插件工具的中文名、原始 ID、驱动 LLM、触发频次、Token 及费用。
- `GET /logs`：中转站调用原始流水明细，支持 `range=today|yesterday` 时间窗与翻页。
- `GET /card`：手账卡片 PNG 的 Base64 Data URL，方便前端直接渲染预览。
